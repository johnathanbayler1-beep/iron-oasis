# Iron Oasis — Session Handoff (2026-07-29)

## 1. Work Completed

- **ScrollExperience.tsx unified under single GSAP ScrollTrigger.** One master pin now drives the 3D frame sequence, layout, and clip-path reveals instead of competing triggers. Fixed z-index stacking and injected Apple-style clip-path reveals over the 3D frame sequence.
- **Black void at page tail fixed.** Root cause was the scroll pin releasing before content underneath was ready; pin release point adjusted to eliminate the empty frame.
- **3D crystal material normal-map bugs fixed.** Removed loop-subdivision applied to non-manifold geometry (was corrupting normals/shading). Material processing now preserves `normalMap` from source (`src?.normalMap ?? null`, `components/ScrollExperience.tsx:86`) and renders `THREE.DoubleSide` (`components/ScrollExperience.tsx:96`) instead of subdividing.
- **Pricing tiers verified live**: Oasis Lite $99, Oasis Plus $125 (Most Popular), Oasis Max $149 — confirmed in `components/ScrollExperience.tsx:177-179`.
- **InvestmentPortal component removed.** Confirmed zero remaining references anywhere in the codebase (component and all call sites gone).

## 2. Current State & Verifications

- `npx tsc --noEmit` passes clean — no type errors.
- Layout and typography verified with no text overlap via Playwright screenshots.
- Scroll milestones verified at **1800px** (entrance phase) and **2198px** (~50% mark) — card entrance timing and 3D frame rendering quality confirmed correct.
- Dev server runs clean at `localhost:3000`.
- 7 token-efficiency tools/plugins loaded and active (claude-mem, context-mode, headroom, serena, rtk, caveman, ponytail).
- Uncommitted change outstanding: `components/ScrollExperience.tsx` (material/geometry refactor above) — not yet committed.

## 3. Unverified Work / Known Issues

- Unused `LoopSubdivision` import may still remain in `ScrollExperience.tsx` post-refactor — needs a cleanup pass.
- `AgentStormChat.tsx:13` chatbot copy still says "Monthly access" — should be checked against banned-vocabulary list (Membership/Monthly framing may conflict with brand protocol's app-only, session-based positioning).
- No mobile/responsive-viewport screenshot verification done this session (only desktop viewport at 1800px/2198px scroll positions).
- Booking flow (AppShowcase.tsx) not exercised this session — built earlier (Jul 24) but not re-verified against current ScrollExperience changes.
- No cross-browser check (Playwright run was Chromium only).

## 4. Next Safe Action

1. Commit the pending `ScrollExperience.tsx` changes (material fix + pin unification), then remove the dead `LoopSubdivision` import as a small follow-up commit.
2. Review `AgentStormChat.tsx:13` copy against the CLAUDE.md banned-vocabulary list ("Monthly" reads close to "Membership") before touching booking-flow or US-expansion work.
