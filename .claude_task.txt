Execute a complete UI, UX, and GSAP architecture overhaul on ScrollExperience.tsx using the B.L.A.S.T. framework. Adhere to local skills from .agents/skills: apple-design, ui-ux-pro-max, emilkowalski-motion, gsap-scrolltrigger, threejs, high-end-visual-design, and impeccable-design-polish. Fix these exactly: 
1. GSAP TIMELINE & VOIDS: Mathematically separate the logo and text phases. The logo mesh MUST reach autoAlpha: 0 and visible: false BEFORE the hero text staggers in. Eliminate dead black scroll voids between sections by tightening the end trigger calculations. Use scrub: true. 
2. 3D GYM SPIN: Lower the Three.js ambient light and adjust directional lighting for a cinematic, high-contrast mood. Restore the sweeping, downward-angled camera fly-through using CatmullRomCurve3. Destroy the key mesh before the gym spin crossfades. Preload GLB assets to eliminate lag. 
3. TYPOGRAPHY: Scale hero and "Move Through The Space" text down. Apply crisp text-white with heavy tracking (tracking-tight for headers, tracking-widest for subheaders). 
4. PRICING CARDS: Overwrite styles with true crystal glassmorphism: bg-white/5 backdrop-blur-[50px] border border-white/20 shadow-[0_8px_32px_rgba(0,0,0,0.5)] shadow-[inset_0_1px_1px_rgba(255,255,255,0.4)] rounded-3xl. All text must use text-white with drop-shadow-md. DOM HIERARCHY INVERSION: Move the price block to the absolute bottom of the card's flex container using mt-auto. The prices must strictly be $99, $125, and $149, and must be the smallest visual element (text-xs text-white/50 tracking-widest uppercase). Verify with tsc and run a live Chrome test.
